# node_modules — исключаем полностью. И папка .prisma/client внутри неё тоже отсекается!
node_modules/
.pnpm-store/

# Next.js — исключаем папки локального кэша и сборки (включая кэш Turbopack)
.next/
out/
build/

# Базы данных и Docker
.pages/
postgres_data/

# Логи и кэш систем разработки
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.turbo

# СЕКРЕТЫ И НАСТРОЙКИ (Критически важно!)
# Мы никогда не пушим файлы .env, чтобы ваши пароли от Postgres 
# и ключи GIGACHAT_CREDENTIALS не украли хакеры на GitHub!
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Системные файлы Windows / macOS
.DS_Store
Thumbs.db
